[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "verified-storefront-orders"
version = "0.1.0"
description = "A typed Python signup and order workflow that verifies customer email before checkout."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "pydantic[email]>=2.9,<3",
  "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
test = [
  "httpx>=0.27,<1",
  "pytest>=8.3,<9",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
